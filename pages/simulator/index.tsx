import styled from "styled-components";

const Simulator = () => {
  return (
    <div>
      <h1>Portfolio simulator & Analysis Tool</h1>
    </div>
  );
};

const SimulatorContainer = styled.div`
  display: flex;
  flex-direction: column;
`;

export default Simulator;
